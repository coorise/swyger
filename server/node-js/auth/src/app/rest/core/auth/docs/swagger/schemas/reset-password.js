export default {
    "type": "object",
    "required": [
        "password",
    ],
    "properties": {
        "password": {
            "type": "string",
            "format": "password"
        }
    }
}